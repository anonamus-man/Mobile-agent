<resources>
    <!-- Shown by the system from the moment the icon is tapped until the first
         frame is drawn. Inherits from the SplashScreen theme so Android 12+
         uses the platform animation and older versions get the compat one. -->
    <style name="Theme.MobileAgent.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.MobileAgent</item>
    </style>

    <style name="Theme.MobileAgent" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">#0B0E14</item>
        <item name="android:navigationBarColor">#0B0E14</item>
        <item name="android:windowActionModeOverlay">true</item>
    </style>
</resources>
